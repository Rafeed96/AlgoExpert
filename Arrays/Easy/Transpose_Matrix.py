def transposeMatrix(matrix):
    # Write your code here.
    mat = []
    n = len(matrix)
    for i in range(0,n):
        print(len(matrix[i]))





matrix = [
    [1, 2, 3],
    [4, 5, 6]
]

transposeMatrix(matrix)